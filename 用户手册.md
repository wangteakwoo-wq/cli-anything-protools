# cli-anything-protools 用户手册

本手册说明如何安装、配置和使用命令行工具控制 Avid Pro Tools，以及使用时的注意事项和常见问题。

---

## 一、简介

**cli-anything-protools** 是一个在终端或脚本里控制 Pro Tools 的命令行工具，适合：

- 用 AI 助手（如 OpenClaw、Claude）发指令控制 Pro Tools  
- 自动化重复操作（建轨、保存、导出等）  
- 在无图形界面的环境下通过脚本驱动 Pro Tools  

**控制方式概览：**

| 方式 | 用途 | 说明 |
|------|------|------|
| **HUI/MIDI** | 推子、声像、静音/独奏、播放/停止等 | 通过 IAC 虚拟 MIDI 与 Pro Tools 通信，实时控制 |
| **AppleScript** | 菜单、对话框、快捷键 | 模拟键盘和点击，完成建工程、建轨、导出等 |
| **AAF** | 离线建工程结构 | 用 JSON 描述轨道/标记，生成 AAF 再导入 Pro Tools |

---

## 二、环境要求

- **系统**：macOS（依赖系统自带的 AppleScript 和 IAC Driver）  
- **Pro Tools**：已安装（Ultimate / Studio / Artist 均可）  
- **Python**：3.10 或更高  
- **权限**：需在「系统设置」中为 **终端** 或 **Cursor** 等调用本工具的程序开启 **辅助功能（Accessibility）**，否则无法发送按键、控制 Pro Tools 界面  

---

## 三、安装

### 3.1 用虚拟环境（推荐）

```bash
cd ~/Desktop/cli-anything-protools
python3 -m venv .venv
source .venv/bin/activate
pip install -e protools/agent-harness/
```

### 3.2 验证

```bash
which cli-anything-protools
cli-anything-protools --help
cli-anything-protools info
```

若 `info` 能显示 Pro Tools 路径、版本、IAC 端口等，说明安装正常。

---

## 四、首次配置

### 4.1 开启 IAC Driver（HUI 用）

1. 打开 **音频 MIDI 设置**（/应用程序/实用工具/）  
2. 菜单 **窗口 → 显示 MIDI 工作室**  
3. 双击 **IAC Driver**  
4. 勾选 **「设备已联网」**，点 **应用**  

未开启 IAC 时，transport、mix、meter 等 HUI 相关命令会不可用或报错。

### 4.2 在 Pro Tools 里启用 HUI

1. Pro Tools 菜单 **Setup → Peripherals → MIDI Controllers**  
2. Type 选 **HUI**  
3. Receive From / Send To 都选 **IAC Driver Bus 1**  
4. 确定  

### 4.3 辅助功能权限（必须）

若用 **终端** 或 **Cursor** 运行本工具，需要允许其控制电脑：

1. **系统设置 → 隐私与安全性 → 辅助功能**  
2. 添加并勾选 **终端** 和/或 **Cursor**（你实际运行命令的那个应用）  
3. 若提示「不允许发送按键」，多半是这里未勾选或未重启应用  

未授权时，建工程、建轨、保存等依赖按键/点击的操作会失败。

---

## 五、换电脑移植指南

换到新电脑后，按下面步骤把本工具「搬过去」，在新机上重新建环境即可。

### 5.1 从旧电脑带走的

- **整个项目文件夹**  
  例如把 `cli-anything-protools` 整个拷到 U 盘或网盘（或 git 仓库）。  
  - 建议**不要**拷贝里面的 `.venv` 文件夹：虚拟环境跟本机 Python 路径绑定，换电脑后通常无法直接用，在新机重建更快更稳。

- **可选：快捷方式配置**  
  若你在旧电脑的 `~/.bash_profile` 或 `~/.zshrc` 里加了 `ptx` / `ptxoff`，把这两行记下来或复制到新机同一文件里（路径要改成新机上的项目位置，见下）。

### 5.2 在新电脑上要装的

1. **安装 Python 3.10+**  
   - 用 [python.org](https://www.python.org/downloads/) 或 Homebrew：`brew install python@3.12` 等。  
   - 终端里执行 `python3 --version` 确认版本 ≥ 3.10。

2. **把项目放到新机**  
   - 例如放到桌面：`~/Desktop/cli-anything-protools`，或你习惯的目录。

3. **在新机重新建虚拟环境并安装**  
   ```bash
   cd ~/Desktop/cli-anything-protools   # 改成你实际路径
   python3 -m venv .venv
   source .venv/bin/activate
   pip install -e protools/agent-harness/
   ```

4. **验证**  
   ```bash
   cli-anything-protools info
   ```  
   能显示 Pro Tools 路径和 IAC 端口即说明安装正常（新机也要装 Pro Tools）。

5. **在新机做一遍「首次配置」**  
   - **四、首次配置** 里的步骤都要在新电脑重做一次：  
     - 开启 IAC Driver  
     - 在 Pro Tools 里设 HUI → IAC Driver Bus 1  
     - 辅助功能里勾选终端 / Cursor 等  

6. **（可选）在新机加 ptx / ptxoff**  
   若项目在新机路径是 `~/Desktop/cli-anything-protools`，在 `~/.bash_profile` 或 `~/.zshrc` 里加上：  
   ```bash
   alias ptx="source ~/Desktop/cli-anything-protools/.venv/bin/activate && echo \"Pro Tools CLI 就绪。输入: cli-anything-protools --help\""
   alias ptxoff="deactivate 2>/dev/null; echo \"Pro Tools CLI 已关闭。\""
   ```  
   若项目在别的目录，把 `~/Desktop/cli-anything-protools` 改成实际路径。保存后执行 `source ~/.bash_profile` 或 `source ~/.zshrc`。

### 5.3 小结

| 步骤 | 说明 |
|------|------|
| 拷贝项目 | 拷整个文件夹，可不拷 `.venv` |
| 新机装 Python | 3.10+ |
| 新机建 venv + 安装 | `python3 -m venv .venv` → `pip install -e protools/agent-harness/` |
| 新机配置 | IAC Driver、Pro Tools HUI、辅助功能权限 |
| 可选 | 在新机改路径后加上 ptx/ptxoff 别名 |

这样在新电脑上就有一份可用的 Pro Tools CLI 环境了。

---

## 六、快捷方式：ptx / ptxoff

若已在 `~/.bash_profile` 或 `~/.zshrc` 里加过：

```bash
alias ptx="source ~/Desktop/cli-anything-protools/.venv/bin/activate && echo \"Pro Tools CLI 就绪。输入: cli-anything-protools --help\""
alias ptxoff="deactivate 2>/dev/null; echo \"Pro Tools CLI 已关闭。\""
```

- **打开环境**：终端里输入 `ptx`，再使用 `cli-anything-protools` 命令  
- **关闭环境**：输入 `ptxoff`  

这样不用每次手动 `source .venv/bin/activate`。

---

## 七、命令总览

### 7.1 查看帮助

```bash
cli-anything-protools --help
cli-anything-protools transport --help
cli-anything-protools mix --help
# 其他组同理
```

### 7.2 命令分组一览

| 命令组 | 作用 | 依赖 |
|--------|------|------|
| `info` | 查看 Pro Tools 与 IAC 状态 | 无 |
| `transport` | 播放、停止、回零、循环、入/出点 | HUI |
| `mix` | 推子、声像、静音、独奏、选择、翻页、电平 | HUI |
| `meter` | 读取电平与峰值 | HUI |
| `session` | 新建/打开/保存/关闭工程 | AppleScript |
| `track` | 新建/删除/复制/重命名/激活轨道 | AppleScript + HUI(选择) |
| `edit` | 剪切/粘贴/分割/淡入淡出/微调等 | AppleScript + 部分 HUI |
| `bounce` | 缩混到磁盘、导出片段 | AppleScript |
| `automation` | 自动化模式、暂停自动化 | HUI |
| `plugin` | 旁通插件等 | AppleScript |
| `io` | I/O 设置、发送分配 | HUI |
| `marker` | 记忆位置新建/召回/窗口 | AppleScript |
| `group` | 编组新建/切换/暂停 | AppleScript + HUI |
| `nav` | 缩放、调音台/编辑窗口切换 | AppleScript + HUI |
| `import` | 导入音频、工程数据 | AppleScript |
| `build-aaf` | 从 JSON 模板生成 AAF 文件 | 本地文件，不依赖 PT 运行 |

### 7.3 JSON 输出（给脚本/AI 用）

加 `--json` 可得到结构化输出，便于解析：

```bash
cli-anything-protools --json info
cli-anything-protools --json mix meters
```

---

## 八、常用命令示例

### 8.1 系统与状态

```bash
cli-anything-protools info
```

### 8.2 走带（transport）

```bash
cli-anything-protools transport play
cli-anything-protools transport stop
cli-anything-protools transport rtz
cli-anything-protools transport loop
cli-anything-protools transport mark-in
cli-anything-protools transport mark-out
cli-anything-protools transport locate 01:00:00:00
```

### 8.3 混音（mix）

```bash
# 推子 0~1，声道 1 起
cli-anything-protools mix fader 1 0.75
cli-anything-protools mix fader-db 1 -6.0

# 声像 -1 左 ~ 1 右；负值前可加 -- 避免被当成选项
cli-anything-protools mix pan 1 -0.5
cli-anything-protools mix pan 2 0.0

cli-anything-protools mix mute 1
cli-anything-protools mix solo 2
cli-anything-protools mix select 1
cli-anything-protools mix bank-left
cli-anything-protools mix bank-right
cli-anything-protools mix meters
```

### 8.4 工程（session）

```bash
# 新建工程（会打开 Dashboard，名称用剪贴板填入）
cli-anything-protools session new -n "我的工程" --sr 48000 --bd 24 --file-type BWF

# 保存 / 另存为 / 关闭
cli-anything-protools session save
cli-anything-protools session save-as ~/Desktop/备份.ptx
cli-anything-protools session close

# 打开已有工程
cli-anything-protools session open ~/Desktop/我的工程.ptx

cli-anything-protools session info
```

### 8.5 轨道（track）

```bash
# 新建轨道：类型 audio|aux|master|vca|midi|instrument|video，格式 mono|stereo|5.1|7.1 等
cli-anything-protools track new -t audio -f 5.1
cli-anything-protools track new -t audio -f stereo
cli-anything-protools track new -t video
cli-anything-protools track new -t audio -f mono -n "对白 1"

# 需先选中轨道（在编辑窗选好，或靠 HUI 选择）
cli-anything-protools track rename "DX 1"
cli-anything-protools track duplicate
cli-anything-protools track inactive
cli-anything-protools track active
cli-anything-protools track delete
```

### 8.6 编辑（edit）

```bash
cli-anything-protools edit select-all
cli-anything-protools edit cut
cli-anything-protools edit copy
cli-anything-protools edit paste
cli-anything-protools edit delete
cli-anything-protools edit separate
cli-anything-protools edit heal
cli-anything-protools edit undo
cli-anything-protools edit redo
cli-anything-protools edit consolidate
cli-anything-protools edit fade-in
cli-anything-protools edit fade-out
cli-anything-protools edit crossfade
cli-anything-protools edit nudge-fwd -n 3
cli-anything-protools edit nudge-bwd
cli-anything-protools edit tab-fwd
cli-anything-protools edit tab-bwd
cli-anything-protools edit trim-start
cli-anything-protools edit trim-end
cli-anything-protools edit strip-silence
cli-anything-protools edit spot
```

### 8.7 导出（bounce）

```bash
cli-anything-protools bounce disk --file-type BWF --bit-depth 24 --sr 48000
cli-anything-protools bounce clips
```

### 8.8 自动化、插件、I/O、标记、编组、导航

```bash
cli-anything-protools automation mode read
cli-anything-protools automation mode touch
cli-anything-protools automation suspend

cli-anything-protools plugin bypass-all

cli-anything-protools io setup
cli-anything-protools io output
cli-anything-protools io input

cli-anything-protools marker new -n "场景1"
cli-anything-protools marker recall 1
cli-anything-protools marker window

cli-anything-protools group new -n "对白组"
cli-anything-protools group toggle
cli-anything-protools group suspend

cli-anything-protools nav zoom-in
cli-anything-protools nav zoom-out
cli-anything-protools nav zoom-fill
cli-anything-protools nav mix-window
cli-anything-protools nav edit-window
```

### 8.9 从 JSON 模板生成 AAF

```bash
# 模板需包含 name, sample_rate, bit_depth, fps, tracks, markers 等
cli-anything-protools build-aaf /path/to/template.json -o /path/to/output.aaf
```

模板中 `markers` 的时间为**秒**（浮点数），例如：

```json
{
  "name": "我的AAF",
  "sample_rate": 48000,
  "bit_depth": 24,
  "fps": 24.0,
  "tracks": [
    {"name": "A1", "format": "mono"},
    {"name": "A2", "format": "stereo"}
  ],
  "markers": [
    {"name": "头", "time": 0.0},
    {"name": "尾", "time": 120.5}
  ]
}
```

---

## 九、注意事项与限制

### 9.1 通用

- **Pro Tools 需已启动**：除 `build-aaf` 和 `info`（部分）外，其他命令都要求 Pro Tools 已打开。  
- **前台与焦点**：依赖按键、菜单的操作会先把 Pro Tools 提到前台；若用户同时用鼠标点别的窗口，可能打断或点错。  
- **版本与语言**：菜单、对话框文字随 Pro Tools 版本和系统语言变化，若你用的是非英文界面，部分对话框标题可能与代码中的英文不一致，导致等待超时或点不到按钮。  

### 9.2 HUI/MIDI

- **一次 8 轨**：HUI 当前只显示 8 个通道，更多轨道需用 `mix bank-left` / `mix bank-right` 翻页。  
- **IAC 未开**：若未启用 IAC 或在 Peripherals 里未选 HUI，transport、mix、meter 会不可用或报错。  

### 9.3 AppleScript / 界面

- **新建工程**：会打开 Pro Tools 的 Dashboard，工程名通过剪贴板粘贴填入；若 Dashboard 布局因版本不同而变化，可能需调整。  
- **新建轨道**：通过「New Tracks」对话框用键盘上下键选类型/格式；类型与格式顺序因版本可能略有差异。  
- **删除轨道**：会尝试点击确认对话框的「Delete」或「OK」；若界面语言不同，按钮名称可能需在代码中适配。  
- **Bounce/导出**：空工程或无选中输出时，可能无法真正导出；Bounce 对话框标题（如 "Bounce Mix"）因版本可能不同。  

### 9.4 功能边界

- **不能读取 .ptx 内容**：无法直接解析工程文件里的轨道列表、片段等。  
- **插件参数**：只能做插入/旁通等，不能调节插件内部参数。  
- **部分菜单**：如「Export → Clips as Files...」等在不同版本中可能改名或不存在，对应命令可能报错。  

---

## 十、常见问题

**Q：提示「不允许发送按键」或 osascript 报错 1002**  
A：在 **系统设置 → 隐私与安全性 → 辅助功能** 中，为运行命令的应用（终端/Cursor 等）勾选并允许控制电脑，必要时重启该应用。

**Q：`transport play` 或 `mix fader` 没反应**  
A：检查 IAC Driver 是否已启用，Pro Tools 里 Setup → Peripherals → MIDI Controllers 是否设为 HUI 且收发均为 IAC Driver Bus 1。

**Q：`session new -n "名字"` 建出来的工程名是空的或是 Untitled**  
A：新建工程名通过剪贴板粘贴写入；若粘贴失败，可能是 Dashboard 文本框未获得焦点，可尝试先手动点一下工程名输入框再重试命令。

**Q：`track new -t video` 或 5.1 轨道创建失败**  
A：确认 Pro Tools 已在前台且没有卡在其它对话框；若版本过新/过旧，New Tracks 里类型或格式顺序可能不同，需要对照实际界面调整。

**Q：负的声像或 dB 值报错，例如 `mix pan 1 -0.5`**  
A：若被解析成选项，可在前面加 `--`，例如：`cli-anything-protools mix pan -- 1 -0.5`；新版已尽量支持直接写负值。

**Q：想用 AI 助手控制 Pro Tools，应该怎么用？**  
A：先在本机安装并配置好本工具（含 ptx、IAC、辅助功能）；在 AI 助手中以自然语言描述需求，由助手转换为对应的 `cli-anything-protools` 命令并在你本机终端执行；执行时需保持 Pro Tools 打开且尽量不切换前台窗口。

---

## 十一、交互式 REPL

不跟子命令直接运行可进入交互式 REPL，逐条输入命令：

```bash
cli-anything-protools
```

进入后输入 `help` 可看命令提示，输入 `quit` 或 `exit` 退出。

---

## 十二、文档与项目结构

- 本手册：`用户手册.md`（使用说明与注意事项）  
- 英文 README：`protools/agent-harness/cli_anything/protools/README.md`（安装与命令表）  
- 架构说明：`protools/agent-harness/PROTOOLS.md`（三层控制架构与能力边界）  

若你修改或扩展了命令，建议同步更新本手册和 README 中的示例与注意事项。
